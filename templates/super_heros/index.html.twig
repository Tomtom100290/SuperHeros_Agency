{% extends 'base.html.twig' %}

{% block title %}SuperHeros index
{% endblock %}

{% block body %}
	<h1>SuperHeros 🗲</h1>
	<div class="SupH">
		{% for super_hero in super_heros %}

			<ul class="Baniere">

				<div class="ImageImport " id="badge"><img src="{{ asset('images/heros/' ~ super_hero.imageName) }}" alt="{{ super_hero.Nom }}"></span>
			</div>
			<li id="bar">
				<span class="progress progress-bar" role="progressbar" aria-label="Example with label" style="width: {{super_hero.Energie}}px" p ; aria-valuenow="50" aria-valuemin="0" aria-valuemax="100">{{ super_hero.Energie }}</li>
			</li>
			<li>{{ super_hero.Nom }}</li>
		</ul>
	{% else %}
		<tr>
			<td colspan="9">no records found</td>
		</tr>
	{% endfor %}
</div>
<div class="separateur"></div>

<div id="blocTable" class="table-responsive-sm">
	<table class="table table-striped table-dark">
		<thead>
			<tr>
				<th>Id</th>
				<th>Nom</th>
				<th>AlterEgo</th>
				<th>Disponible</th>
				<th>Energie</th>
				<th>Biographie</th>
				<th>ImageName</th>
				<th>CreatedAt</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for super_hero in super_heros %}
				<tr>
					<td>{{ super_hero.id }}</td>
					<td>{{ super_hero.Nom }}</td>
					<td>{{ super_hero.AlterEgo }}</td>
					<td>{{ super_hero.Disponible ? 'Disponible' : 'Indisponible' }}</td>
					<td>{{ super_hero.Energie }}</td>
					<td>{{ super_hero.Biographie }}</td>
					<td>{{ super_hero.ImageName }}</td>
					<td>{{ super_hero.createdAt ? super_hero.createdAt|date('Y-m-d H:i:s') : '' }}</td>
					<td class="Actions">
						<a href="{{ path('app_super_heros_show', {'id': super_hero.id}) }}">
							<button class="bton">Voir</button>
						</a>
						<a href="{{ path('app_super_heros_edit', {'id': super_hero.id}) }}">
							<button id="bton2" class="bton">Modifier</button>
						</a>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="9">no records found</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
	<button class="bton">
		<a href="{{ path('app_super_heros_new') }}">Ajouter un Super héros</a>
	</button>
</div>{% endblock %}
